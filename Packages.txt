virtualenv 20.17.1

Python 3.10.8
Django-4.1.4                                    version on kovPC server smartshuk (venv): 4.1.7
pip install psycopg2 # psycopg2-binary          version on server(venv): psycopg2-binary-2.9.5
pip install django_redis                        version on server(venv): installed async-timeout-4.0.2 django_redis-5.2.0 redis-4.5.2
pip install python-dotenv                       version on server(venv): python-dotenv-1.0.0
    # for SECRET_KEYS
pip install pillow                              version on server(venv): pillow-9.4.0
pip install django-location-field               version on server(venv): django-location-field-2.1.1 six-1.16.0
pip install boto3                               version on server(venv): boto3-1.26.95 botocore-1.29.95 jmespath-1.0.1 python-dateutil-2.8.2 s3transfer-0.6.0 urllib3-1.26.15
pip install django-storages                     version on server(venv): django-storages-1.13.2

(IPONE IMAGE FORMAT)
sudo apt-get install libheif-dev libde265-dev libx265-dev       # xcode-select --install # brew install libheif libde265 x265
#pip install pyheif
pip install pillow_heif      


# Find the most likely city name based on the user's input
# from fuzzywuzzy import process # work on top of python-Levenshtein
pip install fuzzywuzzy ( should include the MIT License and give appropriate credit. )
pip install python-Levenshtein (The GPL v2 License requires you to distribute your software under the same license if you include python-Levenshtein as a dependency. This means that your software must also be open-source and shareable under the same terms.)

# https://www.google.com/recaptcha/admin/create
pip install captcha ?
pip install django-recaptcha